server:
  port: 8001
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: SAA-01HelloWorld
  ai:
    # ============Spring AI Alibaba Config============
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:}
      #新版本不需要这个url
#      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      chat:
        options:
          model: deepseek-v4-pro #可以不写.使用默认的

    # ============Spring AI Config============
#    openai:
#      api-key: ${OPENAI_API_KEY:}
